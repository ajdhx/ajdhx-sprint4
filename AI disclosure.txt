
AI disclosure:

The following prompts were used with an AI language model during the development of this work.

1. Given the screenshot provided (of sample GUI), can you create a program that replicates the GUI using tkinter
2. Yes (to implementation plan)
3. Great, I'm not familiar with python classes - can you create the class for the logic as described here:
Choosing the board size and type (note: the types are outlined in the gui: english, hexagon, and diamond)
starting a new game, making a move, determining if a game is over. The following is a sample GUI layout.
The implementation of a GUI is required. You should make every attempt to make your code easy to extend. It
is required to separate the user interface code and the game logic code into different classes (refer to the TicTacToe
example). xUnit tests are required.
4. Yes (to implementation plan) 
5. can you explain how the code works? what's with all the selfs? I'm used to C++. 
6. I added two tests from chatgpt in addition to the existing ones. Why aren't they working (only shows 6 successful, not 8)
7. i see 9 test cases here but only 8 in the test class - which one is missing? (screenshot of sprint1 table attached)
8. can you replace the current test cases (save the chatgpt ones which should just be modified) to make it so it tests each 
of these 9 user stories from the table
9. Yes (to implementation plan)
10. Please update comments and spacing (not including AI disclosure) to the PEP 8 style guide. Brief comments are best. 


Note: The chatgpt tests were working under the original framework, but that was updated in part 8, where the 9 acceptance
criteria from sprint 1 replaced the original tests. Fortunately the chatgpt tests were already testing 4.2 and 5.1, so they
weren't modified significantly by claude.
